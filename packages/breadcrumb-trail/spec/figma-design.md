# BreadcrumbTrail Figma Design

<!--
Traceability document linking the Figma design to the component specification.
Generated by create-component-web-component-figma-design skill.

This document records:
- Where the Figma design lives
- How spec elements map to Figma components
- Design token bindings for both Lumo and Aura themes
- Deviations from spec (if any)
-->

## Figma File

**URL:** https://www.figma.com/design/Wefhd9p9STiZ0ukB5syePq
**Page:** BreadcrumbTrail

## Element Mapping

| Spec Element | Figma Component/Frame | Notes |
|---|---|---|
| `<vaadin-breadcrumb-trail>` | Default Trail, Overflow Trail, Icon Trail, Long Labels Trail | Container represented as horizontal auto-layout with item + separator sequences |
| `<vaadin-breadcrumb-item>` | Item text nodes (link color or body text color) | Link items use primary/accent text color; current page uses body text color with medium weight |
| `<vaadin-breadcrumb-trail-overlay>` | Overlay Panel | Shadow-elevated panel showing collapsed item links |
| Separator (`::after`) | `›` text character | Chevron-right represented as text glyph; actual implementation uses `mask-image` SVG |
| Overflow button | Overflow Button frame | Ellipsis (`…`) with optional hover background |
| `prefix` slot | Icon placeholder rectangle | 16x16 (Lumo) or 14x14 (Aura) colored rectangle |

## Theme Coverage

| Theme | Variants Frame | States Row | Token Bindings |
|---|---|---|---|
| Lumo | Yes | Yes | Yes |
| Aura | Yes | Yes | Yes |

## Variant Mapping

| Spec Property/State | Figma Representation | Values | Theme |
|---|---|---|---|
| `path` attribute (link vs span) | Text color: link color vs body text | Link (blue), Current (dark) | Both |
| `current` attribute | Medium font weight, body text color, no separator | Default, [current] | Both |
| `has-overflow` attribute | Overflow button visible with separator | With/without overflow | Both |
| `has-prefix` attribute | Icon placeholder before label text | With/without icon | Both |
| Hover state | Underline decoration, darker link color | Link items only | Both |
| Focus state | 2px focus ring outline (stroke outside) | Link items + overflow button | Both |
| Active state | Underline + darkest link color | Link items only | Both |
| Disabled state | Disabled text color, no interaction | Link items only | Both |
| Expanded overflow | Hover background on overflow button + overlay panel open | Overflow button only | Both |

## Design Token Bindings

### Lumo

| CSS Custom Property | Lumo Token | Figma Approximation | Resolved Value |
|---|---|---|---|
| `--vaadin-text-color` | `--lumo-body-text-color` | `rgb(26, 31, 48)` | contrast-90pct |
| `--vaadin-text-color-secondary` | `--lumo-secondary-text-color` | `rgb(89, 97, 110)` | contrast-70pct |
| `--vaadin-text-color-disabled` | `--lumo-disabled-text-color` | `rgb(171, 181, 194)` | contrast-30pct |
| Link text color | `--lumo-primary-text-color` | `rgb(0, 87, 196)` | hsl(214, 100%, 43%) |
| `--vaadin-focus-ring-color` | `--lumo-primary-color` | `rgb(0, 87, 196)` | hsl(214, 100%, 48%) |
| Separator color | `--lumo-contrast-50pct` | `rgb(115, 125, 140)` | contrast-50pct |
| Item gap | `--lumo-space-s` | 8px | 0.5rem |
| Font size | `--lumo-font-size-m` | 16px | 1rem |
| Line height | `--lumo-line-height-m` | 1.625 | 26px at 16px |
| Font family | `--lumo-font-family` | Inter (Figma fallback) | system-ui, -apple-system, ... |

### Aura

| CSS Custom Property | Aura Token | Figma Approximation | Resolved Value |
|---|---|---|---|
| `--vaadin-text-color` | `--aura-neutral` | `rgb(28, 28, 31)` | oklch(0.2 ...) in light mode |
| `--vaadin-text-color-secondary` | color-mix(text 60-95%) | `rgb(89, 89, 94)` | ~75% text opacity |
| `--vaadin-text-color-disabled` | color-mix(text 30-33%) | `rgb(184, 186, 189)` | ~30% text opacity |
| Link text color | `--aura-accent-text-color` | `rgb(26, 77, 173)` | oklch-derived from accent blue |
| `--vaadin-focus-ring-color` | oklch(from accent ...) | `rgb(26, 77, 173)` | accent-derived |
| Separator color | text-color-secondary | `rgb(115, 117, 122)` | secondary text color |
| Item gap | `--vaadin-gap-s` | 8px | round(base-size * 0.5) |
| Font size | `--aura-font-size-m` | 14px | round(14/16 * 1rem) |
| Line height | `--aura-line-height-m` | 1.4 (~20px) | round(m * 1.4) |
| Font family | `--aura-font-family` | Inter (Figma fallback) | Instrument Sans, system-ui, ... |
| Border radius | `--vaadin-radius-s` | 5px | min(0.25lh, round(3+2)) |

## Deviations from Spec

1. **Separator rendered as text glyph, not mask-image SVG** -- Figma does not support CSS `mask-image`. The separator is represented as a `›` text character. The actual implementation uses a chevron-right SVG via `--vaadin-breadcrumb-trail-separator` with `mask-image` + `currentColor`.

2. **Font family fallback** -- Instrument Sans (Aura's font) is not available in the Figma file. Inter is used as the closest system fallback for both themes. In production, Lumo uses the system font stack and Aura uses Instrument Sans.

3. **Aura oklch colors approximated** -- Aura uses `oklch()` with relative color syntax and `color-mix()` which are dynamically computed in CSS. Figma values are static approximations for default settings (light mode, `--aura-contrast-level: 1`).

4. **Overlay position is inline, not floating** -- In the actual component, `<vaadin-breadcrumb-trail-overlay>` uses the popover API for top-layer rendering and positions relative to the overflow button. In Figma, the overlay panel is placed inline below the breadcrumb trail as a static representation.

5. **Interactive states shown as separate instances** -- CSS pseudo-classes (`:hover`, `:focus`, `:active`) are represented as distinct side-by-side frames rather than interactive states.

6. **No RTL variant shown** -- The spec describes `transform: scaleX(-1)` on separators in RTL mode. This is not represented in the Figma design.

## Created Variants

| Variant | Description | Spec Reference |
|---|---|---|
| Default Trail (Lumo) | 4-item breadcrumb: Home > Products > Electronics > Laptops | Shadow DOM, item rendering |
| Short Trail (Lumo) | 2-item breadcrumb: Home > Dashboard | Minimal trail |
| Overflow Trail (Lumo) | Collapsed items with `…` overflow button | `has-overflow`, overflow detection |
| Icon Trail (Lumo) | Home item with prefix icon placeholder | `prefix` slot, `has-prefix` |
| Long Labels Trail (Lumo) | Long text labels to show horizontal overflow behavior | Overflow detection |
| Default Trail (Aura) | Same as Lumo at 14px font size | Aura token mapping |
| Short Trail (Aura) | Same as Lumo at 14px | Aura token mapping |
| Overflow Trail (Aura) | Same as Lumo with Aura styling | Aura token mapping |
| Icon Trail (Aura) | Same as Lumo with 14x14 icon | Aura token mapping |
| Long Labels Trail (Aura) | Same as Lumo at 14px | Aura token mapping |
| Link Item States (Lumo) | Default, Hover, Focus, Active, Disabled | Item interactive states |
| Link Item States (Aura) | Default, Hover, Focus, Active, Disabled | Item interactive states |
| Current Page States (Both) | Default, [current] | `current` attribute |
| Overflow Button States (Both) | Default, Hover, Focus, Expanded | Overflow button interaction |
| Overflow Overlay Opened (Both) | Trail with overlay panel showing collapsed items + hover | Overlay rendering, collapsed items |
